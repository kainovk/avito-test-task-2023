version: '3'

services:
  postgres:
    container_name: postgres
    image: postgres:11
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    env_file:
      - .env

volumes:
  db_data:
