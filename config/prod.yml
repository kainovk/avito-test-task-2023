env: "prod" # local / dev / prod

http_server:
  address: "0.0.0.0:8080"
  timeout: 4s
  idle_timeout: 60s

storage:
  host: "localhost" # container name
  port: "5432"
  database: "avito_db"
  username: "postgres"
  password: "root"
